import type { LucideIcon } from "lucide-react";
import {
  Network,
  Zap,
  Repeat2,
  Radio,
  ShieldCheck,
  Eye,
  Search,
} from "lucide-react";

/** Icônes des bénéfices — le texte vit dans le dictionnaire
 *  (t.timeSaving.benefits), dans le même ordre. Le dernier est centré. */
export const benefitIcons: LucideIcon[] = [
  Network,
  Zap,
  Repeat2,
  Radio,
  ShieldCheck,
  Eye,
  Search,
];
